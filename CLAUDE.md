## 提交信息编写

参考历史的多行提交信息（不要使用 --oneline 参考以遮蔽历史），保持一致、简明扼要、多行、中文。
多描述业务变更而非内部代码实现细节、若无业务变更则描述具体真实更改。

生成提交信息时，必须以 `parent` 提交作为唯一的参照系，而不是以你刚才所做的内部操作步骤作为参照系。

任何一条变更描述，都必须在 parent 提交的代码状态中找到对应依据：
- 如果所描述的对象（函数、变量、逻辑块）在 parent 中并不存在，那么它的“移动、提取、重构、替换、调整”均不成立——只应按照“本次最终产物相比 parent 新增了什么”来表述。
- 如果所描述的对象在 parent 中已经存在，才可以用移动、提取、重构等词来描述净变更。

简而言之：**不要把你写代码的过程记录成代码的历史。历史只关心两个 commit 之间的差异，不关心这中间你试过几种写法。**

## 提交快捷工作流

仅当被指定撰写提交信息并传到剪贴板时，才不直接执行 `git commit`。撰写提交信息后，通过 `echo '...' | wl-copy` 复制到剪贴板，供开发人员手动粘贴提交。
